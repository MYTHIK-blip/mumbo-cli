# MUMBO-CLI RITUAL DEPENDENCIES
# These packages empower the scrollshell to interpret commands, render glyphs, and channel ritual output.

typer[all]==0.9.0   # Command-line ritual interface
rich==13.5.2        # Styled terminal output, glyph-rendering
